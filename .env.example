# Copia este archivo como .env y completa los valores.
# NUNCA commitees el .env real.

# PostgreSQL (Supabase Transaction Pooler — puerto 6543, IPv4)
# Si no se define, se usa SQLite local (data/db/registros_om.db)
DATABASE_URL=postgresql://postgres.PROJECT_REF:PASSWORD@aws-0-us-west-2.pooler.supabase.com:6543/postgres
